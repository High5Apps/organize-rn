// These must all be strings. Config.ts handles parsing them to correct types.
declare module '@env' {
  // eslint-disable-next-line import/prefer-default-export
  export const ENABLE_DEVELOPER_SETTINGS: string;
}
